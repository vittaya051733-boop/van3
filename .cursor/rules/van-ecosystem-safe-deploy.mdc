---
description: Safe deploy rules for van3 Rider — SELF storage/hosting only
globs: "**/firestore.rules,**/firebase.json,**/scripts/deploy*.ps1"
alwaysApply: false
---

# van3 Rider — ห้าม deploy SHARED resources

Docs: `..\van2\scripts\DEPLOY_GOVERNANCE.md` + `DEPLOY_RISK_MATRIX.md`

## สิทธิ์ van3

- **SELF เท่านั้น:** storage, hosting
- **blocked:** Firestore default, functions

Real-time: `orders`, `riders/{uid}` — deploy SHARED ผิด = หลุดการเชื่อมต่อ

```powershell
..\van2\scripts\deploy-self.ps1 -App van3 -Target storage -ConfirmDeploy "APPROVE:van3:van-merchant" -FinalAcknowledge "YES I UNDERSTAND"
```
